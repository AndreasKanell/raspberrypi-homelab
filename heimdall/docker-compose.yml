services:
  heimdall:
    image: lscr.io/linuxserver/heimdall:latest
    container_name: heimdall
    environment:
      - PUID=1000
      - PGID=1000
      - TZ= <TIMEZONE>
    volumes:
      - ./config:/config 
    ports:
      - "9090:80" 
      - "9443:443"
    restart: unless-stopped